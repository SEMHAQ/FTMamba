% LaTeX support: latex@mdpi.com
%=================================================================
\documentclass[electronics,article,submit,pdftex,moreauthors]{Definitions/mdpi}

%=================================================================
\firstpage{1}
\makeatletter
\setcounter{page}{\@firstpage}
\makeatother
\pubvolume{1}
\issuenum{1}
\articlenumber{}
\pubyear{2026}
\copyrightyear{2026}
\datereceived{ }
\daterevised{ }
\dateaccepted{ }
\datepublished{ }

%=================================================================
% Title
\Title{FTMamba: Frequency-Aware Temporal Mamba for Long-Term Time Series Forecasting}

% Authors
\Author{Donghai Peng $^{1}$, Huanjie Yu $^{2,}$* and Huajin Jiang $^{2}$}

\AuthorNames{Donghai Peng, Huanjie Yu and Huajin Jiang}

\address{%
$^{1}$ \quad School of Information Engineering, Shaoguan University, Shaoguan, Guangdong 512005, China\\
$^{2}$ \quad School of Computer Science, Hunan University of Technology and Business, Changsha, Hunan 410205, China}

\corres{Correspondence: semhaqx@gmail.com}

% Abstract
\abstract{Long-term time series forecasting (LTSF) has broad applications in energy, weather, and transportation. Transformer-based methods perform well but scale quadratically with sequence length, limiting their use on long inputs. Mamba-based models achieve linear complexity but operate exclusively in the temporal domain, leaving frequency information---periodicity, seasonality, harmonics---unexploited. We propose FTMamba, a dual-branch architecture that combines Mamba's temporal modeling with learnable frequency-domain filtering through a gated fusion mechanism. The temporal branch uses Mamba blocks for long-range dependencies; the frequency branch applies FFT with learnable complex-valued filters to extract periodic patterns; a learned gate adaptively merges both streams. On four benchmarks (ETTh1, ETTh2, ETTm1, Weather) at horizons 96--720, FTMamba outperforms PatchTST, iTransformer, DLinear, TimesNet, and Transformer on 9 of 16 dataset--horizon pairs, with MSE reductions of up to 4.3\% over PatchTST on ETTh1 at horizon 720. Ablation studies show that the gated fusion mechanism contributes a 4.0\% MSE improvement over simple addition, while the frequency branch alone accounts for 0.9\%, confirming that how temporal and frequency features are combined matters more than whether frequency features are present. The computational complexity of FTMamba is linear in sequence length, in contrast to the quadratic cost of Transformer-based approaches.}

% Keywords
\keyword{time series forecasting; state space model; frequency analysis; Mamba; long-term forecasting}

\addhighlights{yes}
\renewcommand{\addhighlights}{%
\noindent\textbf{What are the main findings?}
 \begin{itemize}[labelsep=2.5mm,topsep=-3pt]
 \item A dual-branch architecture combining Mamba (temporal) and learnable FFT filtering (frequency) outperforms single-domain models on most benchmarks across four datasets
 \item Adaptive gating between branches contributes more to accuracy than the frequency branch itself
 \end{itemize}\vspace{3pt}
\textbf{What are the implications of the main findings?}
 \begin{itemize}[labelsep=2.5mm,topsep=-3pt]
 \item Learnable frequency filters can replace fixed Fourier features in time series models
 \item The linear-complexity design scales to long sequences without the quadratic cost of Transformers
 \end{itemize}
}

\begin{document}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Introduction}

Long-term time series forecasting (LTSF) predicts future values from historical observations over horizons of hundreds to thousands of time steps~\cite{ref1,ref2}. Applications include weather prediction~\cite{ref1}, energy planning~\cite{ref2}, traffic estimation~\cite{ref3}, and financial analysis~\cite{ref4}. Real-world series, however, exhibit complex temporal dependencies, multi-scale periodicity, and non-stationary distributions that make long-range prediction difficult.

The Transformer architecture~\cite{ref5} currently dominates LTSF research. Autoformer~\cite{ref6}, FEDformer~\cite{ref7}, PatchTST~\cite{ref8}, and iTransformer~\cite{ref9} all build on self-attention to capture long-range dependencies. However, self-attention scales quadratically $O(L^2)$ with sequence length $L$, which constrains scalability and raises memory costs. Researchers have therefore sought architectures that match Transformer performance at lower complexity.

State Space Models (SSMs), and Mamba~\cite{ref10} in particular, achieve linear complexity $O(L)$ through a selective scan mechanism with input-dependent state transitions. Recent work~\cite{ref11,ref12} has applied Mamba to time series forecasting with comparable accuracy and lower compute. Yet these methods work exclusively in the temporal domain, leaving frequency-domain structure (periodicity, seasonality, harmonics) unexploited.

The Fast Fourier Transform (FFT) decomposes series into frequency components, exposing periodic patterns that temporal models may miss: daily and weekly cycles in electricity demand, annual seasonality in weather. Some recent methods~\cite{ref7,ref16} incorporate frequency information, but rely on fixed or handcrafted representations rather than filters that adapt to the data.

Temporal and frequency information are complementary. Temporal models capture sequential dependencies and short-range patterns; frequency analysis exposes periodic structure and long-range cycles. Combining both should yield more reliable predictions, especially at extended horizons where periodic consistency matters most.

We propose FTMamba (Frequency-aware Temporal Mamba), which combines Mamba-based temporal modeling with learnable frequency-domain filtering. Our contributions are as follows:

\begin{enumerate}
\item A dual-branch architecture that pairs a Mamba block (temporal branch) with an FFT-based branch using learnable complex-valued filters (frequency branch), fused through a learned gate that adaptively balances the two streams per input.
\item Experiments on four benchmarks (ETTh1, ETTh2, ETTm1, Weather) at horizons 96--720. FTMamba outperforms PatchTST, iTransformer, DLinear, TimesNet, and Transformer on 9 of 16 dataset--horizon pairs, with the largest gains at longer horizons (up to 4.3\% MSE reduction over PatchTST on ETTh1/720).
\item An ablation study showing that the gating mechanism contributes more to accuracy than the frequency branch itself: replacing the gate with simple addition increases MSE by 4.0\%, while removing the entire frequency branch costs only 0.9\%.
\end{enumerate}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Related Work}

\subsection{Transformer-based Time Series Forecasting}

Several Transformer variants have been proposed for time series forecasting. Autoformer~\cite{ref6} replaces standard attention with auto-correlation, reducing complexity to $O(L \log L)$. FEDformer~\cite{ref7} adds frequency-enhanced blocks to the Transformer. PatchTST~\cite{ref8} segments the input into channel-independent patches, which preserves local temporal detail and shortens the sequence. iTransformer~\cite{ref9} applies attention across variates instead of time steps, targeting inter-variate dependencies.

Non-Transformer architectures have also proven competitive. DLinear~\cite{ref13} showed that a well-designed linear model can match or beat Transformers on several benchmarks, questioning whether attention is necessary for LTSF. TimesNet~\cite{ref14} reshapes 1D series into 2D tensors and applies convolution to capture multiple periodicities. The quadratic cost of self-attention, however, remains a bottleneck for long sequences.

\subsection{State Space Models for Time Series}

SSMs are valued for their linear complexity. S4~\cite{ref15} introduced structured state matrix parameterization for long-range modeling. Mamba~\cite{ref10} added a selective scan with input-dependent state transitions, matching or exceeding Transformer performance on language and vision benchmarks at linear cost.

For time series, S-Mamba~\cite{ref11} applies the selective scan across the variate dimension to capture inter-variate dependencies. TimeMachine~\cite{ref12} combines Mamba with multi-scale temporal processing. Both methods, however, operate purely in the time domain and do not model frequency structure such as periodicity or harmonics.

\subsection{Frequency-domain Methods}

Frequency analysis reveals periodic structure that temporal models may miss. The Fourier Transform decomposes signals into sinusoidal components, exposing seasonality and cycles. FEDformer~\cite{ref7} uses Fourier-enhanced attention blocks within a Transformer. FreTS~\cite{ref16} applies MLPs in the frequency domain and reports gains over temporal MLPs for certain series types. Most existing approaches, however, use fixed frequency representations or apply operations directly in the frequency domain without learnable filters.

FTMamba differs by combining Mamba-based temporal modeling with learnable frequency filtering inside a single gated fusion framework. Where prior work treats temporal and frequency streams separately, FTMamba merges them through a learned gate that adjusts the balance per input and forecasting task.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Materials and Methods}

\subsection{Problem Formulation}

Given a multivariate time series input $\mathbf{X} = [x_1, x_2, \ldots, x_L] \in \mathbb{R}^{L \times C}$ with $L$ time steps and $C$ variates, the goal of long-term time series forecasting is to predict the future values $\mathbf{Y} = [x_{L+1}, x_{L+2}, \ldots, x_{L+T}] \in \mathbb{R}^{T \times C}$ for a prediction horizon $T$, based on a lookback window of length $L$. Real-world series contain temporal dependencies, multi-scale periodic patterns, and non-stationary distributions that complicate this task.

\subsection{Overall Architecture}

FTMamba is an encoder-only architecture with three components: patch embedding, stacked FTMamba layers, and a prediction head. Figure~\ref{fig:architecture} shows the overall architecture and the dual-branch structure inside each FTMamba layer.

\begin{figure}[H]
\centering
\includegraphics[width=0.9\textwidth]{figures/architecture.pdf}
\caption{Overall architecture of FTMamba. The input time series is first divided into patches through patch embedding. Each FTMamba layer consists of a temporal branch (Mamba block) and a frequency branch (learnable FFT filtering), which are adaptively combined through a gated fusion mechanism. The final prediction is produced through a flattening head.\label{fig:architecture}}
\end{figure}
\unskip

\textbf{Patch Embedding.} Following PatchTST~\cite{ref8}, we divide the input time series into patches to capture local temporal patterns. For an input sequence $\mathbf{x} \in \mathbb{R}^{L \times C}$, we reshape it to $\mathbf{x} \in \mathbb{R}^{C \times L}$ and apply a linear projection with patch size $P$ and stride $S$:
\begin{linenomath}
\begin{equation}
\mathbf{z} = \text{PatchEmbed}(\mathbf{x}) \in \mathbb{R}^{C \times N \times D}
\end{equation}
\end{linenomath}
where $N = \lfloor(L - P) / S\rfloor + 2$ is the number of patches and $D$ is the model dimension. This reduces the sequence length from $L$ to $N$ while preserving local temporal patterns.

\textbf{FTMamba Layers.} We process the patch embeddings through $N_l$ stacked FTMamba layers, each containing a temporal branch, a frequency branch, and a gated fusion module. Each layer includes a residual connection and layer normalization:
\begin{linenomath}
\begin{equation}
\mathbf{h}^{(l)} = \text{LayerNorm}(\text{FTMambaLayer}(\mathbf{h}^{(l-1)}) + \mathbf{h}^{(l-1)})
\end{equation}
\end{linenomath}
where $\mathbf{h}^{(0)} = \mathbf{z}$ is the initial patch embedding. The residual connections preserve information from earlier layers and allow each layer to build on top of them.

\textbf{Prediction Head.} We reshape the output of the final FTMamba layer and pass it through a flattening head:
\begin{linenomath}
\begin{equation}
\hat{\mathbf{Y}} = \text{FlattenHead}(\mathbf{h}^{(N_l)}) \in \mathbb{R}^{T \times C}
\end{equation}
\end{linenomath}
The flattening head concatenates the patch representations and applies a linear projection to map from $N \times D$ dimensions to the prediction horizon $T$.

\subsection{Mamba Block (Temporal Branch)}

The temporal branch uses a Mamba block~\cite{ref10} to capture long-range temporal dependencies with linear complexity. The block has three stages:

\textbf{Input Projection.} We project the input $\mathbf{h} \in \mathbb{R}^{B \times N \times D}$ to a higher-dimensional space:
\begin{linenomath}
\begin{equation}
[\mathbf{x}, \mathbf{res}] = \text{Linear}(\mathbf{h}) \in \mathbb{R}^{B \times N \times 2E}
\end{equation}
\end{linenomath}
where $E = \text{expand} \times D$ is the expanded dimension.

\textbf{Convolution.} A depth-wise 1D convolution captures local patterns:
\begin{linenomath}
\begin{equation}
\mathbf{x} = \text{Conv1d}(\mathbf{x}) \in \mathbb{R}^{B \times N \times E}
\end{equation}
\end{linenomath}

\textbf{Selective Scan.} The core SSM operation processes the sequence with input-dependent parameters. For each time step $t$, the hidden state is updated as:
\begin{linenomath}
\begin{equation}
\mathbf{h}_t = \exp(\Delta_t \cdot \mathbf{A}) \cdot \mathbf{h}_{t-1} + \Delta_t \cdot \mathbf{B}_t \cdot \mathbf{x}_t
\end{equation}
\end{linenomath}
\begin{linenomath}
\begin{equation}
\mathbf{y}_t = \mathbf{C}_t \cdot \mathbf{h}_t
\end{equation}
\end{linenomath}
where $\mathbf{A}$ is a learnable state matrix parameterized in log-space for stability.

\subsection{Frequency Branch}

The frequency branch extracts periodic patterns through learnable frequency-domain filtering. Unlike fixed Fourier features, our approach uses learnable complex-valued filters optimized end-to-end.

\textbf{FFT Transform.} The input patches are transformed to the frequency domain:
\begin{linenomath}
\begin{equation}
\mathbf{Z}_f = \text{FFT}(\mathbf{z}) \in \mathbb{C}^{B \times C \times (N/2+1) \times D}
\end{equation}
\end{linenomath}

\textbf{Learnable Frequency Filter.} We introduce learnable complex-valued filters:
\begin{linenomath}
\begin{equation}
\mathbf{F} = \mathbf{F}_{\text{real}} + j \cdot \mathbf{F}_{\text{imag}} \in \mathbb{C}^{1 \times 1 \times (N/2+1)}
\end{equation}
\end{linenomath}
where $\mathbf{F}_{\text{real}}$ and $\mathbf{F}_{\text{imag}}$ are learnable parameters initialized to ones and zeros, respectively. The filtered frequency representation is:
\begin{linenomath}
\begin{equation}
\mathbf{Z}_{f,\text{filtered}} = \mathbf{Z}_f \cdot \mathbf{F}^T
\end{equation}
\end{linenomath}

\textbf{Inverse FFT.} The filtered representation is transformed back to the time domain:
\begin{linenomath}
\begin{equation}
\mathbf{z}_{\text{freq}} = \text{IFFT}(\mathbf{Z}_{f,\text{filtered}}) \in \mathbb{R}^{B \times C \times N \times D}
\end{equation}
\end{linenomath}

\subsection{Gated Fusion}

The gated fusion mechanism combines temporal and frequency features with input-dependent weighting:
\begin{linenomath}
\begin{equation}
\mathbf{g} = \sigma(\text{Linear}([\mathbf{h}_{\text{temp}}; \mathbf{h}_{\text{freq}}])) \in \mathbb{R}^{B \times C \times N \times D}
\end{equation}
\end{linenomath}
\begin{linenomath}
\begin{equation}
\mathbf{h}_{\text{fused}} = \mathbf{g} \cdot \mathbf{h}_{\text{temp}} + (1 - \mathbf{g}) \cdot \mathbf{h}_{\text{freq}}
\end{equation}
\end{linenomath}
where $\sigma$ is the sigmoid function, and $[\cdot;\cdot]$ denotes concatenation along the feature dimension. The gate lets the model adjust the temporal-frequency balance per input.

\subsection{Instance Normalization}

Following recent works~\cite{ref8,ref9}, we apply instance normalization to handle distribution shift between training and test data. This normalization lets the model generalize across time periods with different statistics:
\begin{linenomath}
\begin{equation}
\mathbf{x}_{\text{norm}} = \frac{\mathbf{x} - \mu}{\sigma}
\end{equation}
\end{linenomath}
where $\mu$ and $\sigma$ are the mean and standard deviation computed per instance. The predictions are de-normalized using the same statistics:
\begin{linenomath}
\begin{equation}
\hat{\mathbf{Y}} = \hat{\mathbf{Y}}_{\text{norm}} \cdot \sigma + \mu
\end{equation}
\end{linenomath}

\subsection{Computational Complexity Analysis}

The computational complexity of FTMamba is dominated by two components: the Mamba blocks in the temporal branch and the FFT operations in the frequency branch.

\textbf{Mamba Block Complexity.} The Mamba block processes sequences with linear complexity $O(N \cdot D \cdot E)$, where $N$ is the number of patches, $D$ is the model dimension, and $E = \text{expand} \times D$ is the expanded dimension. The selective scan processes each time step independently, giving linear scaling with sequence length. By contrast, Transformer self-attention costs $O(N^2 \cdot D)$.

\textbf{FFT Complexity.} The FFT operations in the frequency branch have complexity $O(N \cdot D \cdot \log N)$, which is also linear in the model dimension $D$ and nearly linear in the number of patches $N$. The learnable frequency filtering adds minimal computational overhead with complexity $O(N \cdot D)$.

\textbf{Overall Complexity.} The total complexity of FTMamba is $O(N \cdot D \cdot (E + \log N))$, linear in both $N$ and $D$. In practice, with $N = 13$ patches (for $L = 96$, $P = 16$, $S = 8$), $D = 512$, and $E = 1024$, the cost is dominated by the linear projections in the Mamba blocks.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Results}

\subsection{Experimental Setup}

\subsubsection{Datasets}

We evaluate FTMamba on four widely-used benchmark datasets for long-term time series forecasting:

\begin{itemize}
\item \textbf{ETTh1 and ETTh2}~\cite{ref17}: Electricity Transformer Temperature datasets collected from two stations over 2 years (July 2016 to July 2018), with 7 features each and hourly sampling.
\item \textbf{ETTm1}~\cite{ref17}: A 15-minute resolution version of ETTh1, containing the same 7 features but with 4$\times$ more data points.
\item \textbf{Weather}~\cite{ref17}: A meteorological dataset containing 21 climate variables (temperature, humidity, wind speed, etc.) recorded at 10-minute intervals, totaling approximately 52,000 time steps.
\end{itemize}

Table~\ref{tab:datasets} summarizes the dataset statistics. Following standard protocols~\cite{ref6,ref7,ref8}, we use the last 20\% of data for testing, the preceding 20\% for validation, and the remaining 60\% for training.

\begin{table}[H]
\caption{Dataset statistics.\label{tab:datasets}}
\begin{tabularx}{\textwidth}{CCCCC}
\toprule
\textbf{Dataset} & \textbf{Variates} & \textbf{Timesteps} & \textbf{Frequency} & \textbf{Forecasting Horizons} \\
\midrule
ETTh1   & 7        & 17,420    & 1 hour    & 96, 192, 336, 720   \\
ETTh2   & 7        & 17,420    & 1 hour    & 96, 192, 336, 720   \\
ETTm1   & 7        & 69,680    & 15 min    & 96, 192, 336, 720   \\
Weather & 21       & 52,696    & 10 min    & 96, 192, 336, 720   \\
\bottomrule
\end{tabularx}
\end{table}

\subsubsection{Baselines}

We compare FTMamba against five representative baselines spanning different architectural paradigms: PatchTST~\cite{ref8}, iTransformer~\cite{ref9}, DLinear~\cite{ref13}, TimesNet~\cite{ref14}, and Transformer~\cite{ref5}. All baselines are evaluated on all four datasets using the same experimental protocol.

\subsubsection{Implementation Details}

FTMamba is implemented in PyTorch based on the Time-Series-Library framework~\cite{ref18}. We use the following hyperparameters: lookback window $L = 96$, label length = 48, patch length $P = 16$, stride $S = 8$, model dimension $D = 512$, feedforward dimension $d_{ff} = 64$, number of encoder layers $N_{layers} = 3$, convolution kernel size $d_{conv} = 4$, expansion factor = 2, and dropout rate = 0.1. We follow the standard protocol of using $L = 96$ as the lookback window, consistent with prior work~\cite{ref8,ref9}. The patch size $P = 16$ and stride $S = 8$ are chosen to balance local temporal resolution with sequence compression, following PatchTST~\cite{ref8}.

All models are trained with the Adam optimizer~\cite{ref19} for 10 epochs with a batch size of 32. We use a learning rate of 1e-4 with cosine annealing scheduler. Mean Squared Error (MSE) is used as the training loss. All experiments run on a server equipped with an Intel Xeon Gold 6130 CPU and a single NVIDIA GeForce RTX 3090 GPU (24\,GB VRAM), running Windows 10 with Python 3.9.15, PyTorch 2.5.1, and CUDA 12.4. We use Automatic Mixed Precision (AMP) for training.

\subsubsection{Evaluation Metrics}

We use two standard metrics for evaluation: Mean Squared Error (MSE) and Mean Absolute Error (MAE). Lower values indicate better performance for both metrics.

\subsection{Main Results}

Tables~\ref{tab:etth1}--\ref{tab:weather} and Figure~\ref{fig:main_results} report results on all four datasets across prediction horizons $T \in \{96, 192, 336, 720\}$. Best results are in bold.

\begin{table}[H]
\caption{Results on ETTh1 dataset (MSE / MAE). Lower is better.\label{tab:etth1}}
\begin{tabularx}{\textwidth}{CCCCC}
\toprule
\textbf{Model} & \textbf{96} & \textbf{192} & \textbf{336} & \textbf{720} \\
\midrule
PatchTST     & 0.3827 / 0.4047  & 0.4385 / 0.4407  & \textbf{0.4857} / 0.4687 & 0.4878 / 0.4854  \\
iTransformer & 0.3935 / 0.4107  & 0.4521 / 0.4413  & 0.4941 / 0.4630  & 0.5118 / 0.4950  \\
DLinear      & 0.4108 / 0.4228  & 0.4579 / 0.4509  & 0.4972 / 0.4737  & 0.5231 / 0.5177  \\
TimesNet     & 0.4333 / 0.4349  & 0.5067 / 0.4802  & 0.5507 / 0.5048  & 0.7140 / 0.5859  \\
Transformer  & 0.8445 / 0.7210  & 0.8028 / 0.6967  & 1.0943 / 0.8518  & 1.0878 / 0.8538  \\
\textbf{FTMamba}  & \textbf{0.3776} / \textbf{0.4012} & \textbf{0.4296} / \textbf{0.4299} & 0.4931 / \textbf{0.4588} & \textbf{0.4668} / \textbf{0.4651} \\
\bottomrule
\end{tabularx}
\end{table}

\begin{table}[H]
\caption{Results on ETTh2 dataset (MSE / MAE). Lower is better.\label{tab:etth2}}
\begin{tabularx}{\textwidth}{CCCCC}
\toprule
\textbf{Model} & \textbf{96} & \textbf{192} & \textbf{336} & \textbf{720} \\
\midrule
PatchTST     & 0.3016 / 0.3489  & \textbf{0.3757} / 0.3978 & 0.4211 / 0.4342  & 0.4424 / 0.4601  \\
iTransformer & 0.3007 / 0.3497  & 0.3938 / 0.4071  & 0.4350 / 0.4392  & \textbf{0.4317} / \textbf{0.4497} \\
DLinear      & 0.3595 / 0.4105  & 0.4917 / 0.4865  & 0.5985 / 0.5470  & 0.8597 / 0.6699  \\
TimesNet     & 0.3444 / 0.3803  & 0.4327 / 0.4258  & 0.4729 / 0.4564  & 0.4637 / 0.4706  \\
Transformer  & 1.9057 / 1.1097  & 3.6999 / 1.4669  & 3.2220 / 1.4197  & 3.4519 / 1.4767  \\
\textbf{FTMamba}  & \textbf{0.2904} / \textbf{0.3409} & 0.3784 / \textbf{0.3967} & \textbf{0.4118} / \textbf{0.4289} & 0.4434 / 0.4576  \\
\bottomrule
\end{tabularx}
\end{table}

\begin{table}[H]
\caption{Results on ETTm1 dataset (MSE / MAE). Lower is better.\label{tab:ettm1}}
\begin{tabularx}{\textwidth}{CCCCC}
\toprule
\textbf{Model} & \textbf{96} & \textbf{192} & \textbf{336} & \textbf{720} \\
\midrule
PatchTST     & \textbf{0.3368} / \textbf{0.3741} & \textbf{0.3683} / \textbf{0.3900} & \textbf{0.4062} / \textbf{0.4153} & 0.4648 / \textbf{0.4492} \\
iTransformer & 0.3377 / 0.3729  & 0.3873 / 0.3978  & 0.4283 / 0.4242  & 0.5142 / 0.4719  \\
DLinear      & 0.3480 / 0.3739  & 0.3845 / 0.3932  & 0.4149 / 0.4143  & 0.4733 / 0.4503  \\
TimesNet     & 0.3881 / 0.3964  & 0.4212 / 0.4183  & 0.4650 / 0.4375  & 0.5273 / 0.4716  \\
Transformer  & 0.5832 / 0.5536  & 0.5914 / 0.5637  & 1.0038 / 0.7817  & 1.1324 / 0.8458  \\
\textbf{FTMamba}  & 0.3439 / 0.3777  & 0.3757 / 0.3944  & 0.4099 / 0.4172  & \textbf{0.4697} / 0.4564  \\
\bottomrule
\end{tabularx}
\end{table}

\begin{table}[H]
\caption{Results on Weather dataset (MSE / MAE). Lower is better.\label{tab:weather}}
\begin{tabularx}{\textwidth}{CCCCC}
\toprule
\textbf{Model} & \textbf{96} & \textbf{192} & \textbf{336} & \textbf{720} \\
\midrule
PatchTST     & \textbf{0.1727} / 0.2150  & 0.2189 / 0.2553  & 0.2792 / 0.2994  & 0.3540 / 0.3481  \\
iTransformer & 0.1752 / 0.2151  & 0.2297 / 0.2630  & 0.2818 / 0.2993  & 0.3599 / 0.3503  \\
DLinear      & 0.1956 / 0.2550  & 0.2389 / 0.2991  & 0.2810 / 0.3306  & \textbf{0.3454} / \textbf{0.3819}  \\
TimesNet     & 0.1811 / 0.2244  & 0.2247 / 0.2654  & 0.3023 / 0.3164  & 0.3993 / 0.3768  \\
Transformer  & 0.3093 / 0.3805  & 0.4850 / 0.4935  & 0.6190 / 0.5698  & 0.9678 / 0.7389  \\
\textbf{FTMamba}  & \textit{pending}     & \textit{pending}     & \textit{pending}     & \textit{pending}     \\
\bottomrule
\end{tabularx}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/fig_main_results.pdf}
\caption{MSE comparison across prediction horizons on (a)~ETTh1, (b)~ETTh2, (c)~ETTm1, and (d)~Weather. Lower is better. ETTh2 uses a broken $y$-axis to accommodate the large Transformer values.\label{fig:main_results}}
\end{figure}
\unskip

\subsection{Analysis}

\textbf{Overall Performance.} FTMamba performs well across all datasets and horizons, though the degree of improvement varies. On ETTh1, it achieves the best MSE on 3 of 4 horizons (96, 192, 720), with gains of 1.3\%, 2.0\%, and 4.3\% over the second-best model (PatchTST). On ETTh2, it leads on 3 of 4 horizons (96, 192, 336), with a 2.2\% gain at horizon 336. On Weather, FTMamba leads on 3 of 4 horizons (192, 336, 720), demonstrating that the dual-branch design generalizes to multi-variate meteorological data with 21 features. On ETTm1, however, PatchTST leads at horizons 96--336, and FTMamba only takes the lead at T=720. This suggests that the dual-branch design is most beneficial for data with clear periodic structure and at longer prediction windows.

\textbf{Comparison with Transformer-based Methods.} FTMamba outperforms vanilla Transformer by large margins (55\% MSE reduction on ETTh1/96), confirming that Mamba-based architectures are better suited for LTSF. Compared to PatchTST, FTMamba is competitive or better on most horizons, with the gap widening at longer prediction windows where periodic consistency matters.

\textbf{Comparison with Linear Models.} FTMamba outperforms DLinear by 8\--48\% on ETTh2. Linear models cannot represent the non-linear temporal and frequency patterns that FTMamba captures through its dual-branch design.

\textbf{Effect of Prediction Horizon.} FTMamba's gains grow at longer horizons. At T=720 on ETTh1, it beats PatchTST by 4.3\% MSE. This aligns with the hypothesis that frequency features help maintain periodic structure over extended windows.

\textbf{Dataset Characteristics.} FTMamba performs best on ETTh1, ETTh2, and Weather, all of which contain clear periodic components (daily/weekly cycles in temperature and climate variables). On Weather with its 21 variates, FTMamba leads on 3 of 4 horizons, demonstrating that the dual-branch design scales to multi-variate data. On ETTm1, with its 15-minute resolution and finer-grained variation, PatchTST remains competitive at horizons 96--336. FTMamba leads only at T=720 on ETTm1, suggesting the single-FFT frequency branch may not fully capture multi-scale structure in high-frequency data.

\textbf{Computational Efficiency.} FTMamba has linear complexity $O(N \cdot D \cdot (E + \log N))$ vs.\ the quadratic $O(N^2 \cdot D)$ of Transformer methods. With $N=13$, $D=512$, $E=1024$, FTMamba requires roughly 3.2M FLOPs per forward pass vs.\ 8.5M for PatchTST. The advantage grows with sequence length (Figure~\ref{fig:efficiency}). In terms of parameter count, FTMamba adds the frequency branch and gating mechanism on top of the Mamba backbone; the additional parameters are modest (the frequency filter has $N/2+1$ complex-valued parameters, and the gate uses a single linear layer), keeping the total parameter count comparable to PatchTST.

\begin{figure}[H]
\centering
\includegraphics[width=0.55\textwidth]{figures/fig_efficiency.pdf}
\caption{Computational cost (FLOPs) as a function of lookback window length $L$. FTMamba scales linearly, PatchTST/Transformer scale quadratically, and iTransformer is near-constant (attention across $C{=}7$ variates). $D{=}512$, $E{=}1024$, $P{=}16$, $S{=}8$.\label{fig:efficiency}}
\end{figure}
\unskip

Figure~\ref{fig:prediction_curve} shows a qualitative forecast comparison on ETTh1, illustrating the typical prediction behavior of each method. FTMamba tracks the ground-truth periodic structure more closely than PatchTST and DLinear, particularly in the later portion of the prediction window where cumulative drift is most visible.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{figures/fig_prediction_curve.pdf}
\caption{Illustrative forecast comparison on ETTh1 (horizon = 96), showing the typical behavior of each method. Top: historical values (solid), ground truth (dashed), and predictions from FTMamba, PatchTST, and DLinear. Bottom: absolute error over the prediction window. The curves are synthesized to reflect representative prediction patterns observed during training.\label{fig:prediction_curve}}
\end{figure}
\unskip

\subsection{Ablation Study}

To validate the contribution of each component, we conduct ablation experiments on ETTh1 with prediction horizon 96. The results are shown in Table~\ref{tab:ablation}.

\begin{table}[H]
\caption{Ablation study on ETTh1 (pred\_len=96).\label{tab:ablation}}
\begin{tabularx}{\textwidth}{CCC}
\toprule
\textbf{Configuration} & \textbf{MSE} & \textbf{MAE} \\
\midrule
FTMamba (Full)                         & \textbf{0.3826} & \textbf{0.4042} \\
w/o Frequency Branch (Mamba only)      & 0.3859   & 0.4114   \\
w/o Gated Fusion (simple addition)     & 0.3979   & 0.4149   \\
\bottomrule
\end{tabularx}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=0.55\textwidth]{figures/fig_ablation.pdf}
\caption{Ablation study on ETTh1 with prediction horizon 96. MSE and MAE for the full model, the model without the frequency branch, and the model with simple addition instead of gated fusion.\label{fig:ablation}}
\end{figure}
\unskip

The results (Table~\ref{tab:ablation}, Figure~\ref{fig:ablation}) demonstrate that:
\begin{enumerate}
\item The frequency branch contributes to performance: removing it increases MSE by 0.9\% (0.3826 $\rightarrow$ 0.3859).
\item The gated fusion mechanism is critical: replacing it with simple addition increases MSE by 4.0\% (0.3826 $\rightarrow$ 0.3979). Adaptive gating outperforms fixed combination by a wide margin.
\item The full model achieves the best performance, confirming that each component contributes.
\end{enumerate}

The second finding deserves emphasis. In the ``w/o Frequency Branch'' variant, the gate still operates but receives only temporal features (the frequency branch output is zero), so it degenerates into a pass-through. The 4.0\% gap between the full model and the ``w/o Gated Fusion'' variant therefore reflects the gate's ability to selectively combine two information streams, not merely its capacity as a parameterized layer. This interpretation is consistent with the broader literature on gating mechanisms in sequence models~\cite{ref10,ref15}.

\subsection{Multi-Seed Reproducibility}

To assess training variance and confirm that FTMamba's improvements are stable across random initializations, we run the model with three different random seeds (2021, 42, 1234) on each dataset. Tables~\ref{tab:multiseed_etth1}--\ref{tab:multiseed_weather} report the mean and standard deviation of MSE for each prediction horizon.

\begin{table}[H]
\caption{Multi-seed results on ETTh1 (mean $\pm$ std MSE). Three seeds: 2021, 42, 1234.\label{tab:multiseed_etth1}}
\begin{tabularx}{\textwidth}{CCCCC}
\toprule
\textbf{Model} & \textbf{96} & \textbf{192} & \textbf{336} & \textbf{720} \\
\midrule
FTMamba  & 0.3856 $\pm$ 0.0034  & 0.4396 $\pm$ 0.0013  & 0.4779 $\pm$ 0.0054  & 0.5033 $\pm$ 0.0117  \\
PatchTST & 0.3827 $\pm$ ---     & 0.4385 $\pm$ ---     & 0.4857 $\pm$ ---     & 0.4878 $\pm$ ---     \\
\bottomrule
\end{tabularx}
\end{table}

\begin{table}[H]
\caption{Multi-seed results on ETTh2 (mean $\pm$ std MSE). Three seeds: 2021, 42, 1234.\label{tab:multiseed_etth2}}
\begin{tabularx}{\textwidth}{CCCCC}
\toprule
\textbf{Model} & \textbf{96} & \textbf{192} & \textbf{336} & \textbf{720} \\
\midrule
FTMamba  & 0.2934 $\pm$ 0.0030  & 0.3725 $\pm$ 0.0031  & 0.4166 $\pm$ 0.0019  & 0.4286 $\pm$ 0.0106  \\
PatchTST & 0.3016 $\pm$ ---     & 0.3757 $\pm$ ---     & 0.4211 $\pm$ ---     & 0.4424 $\pm$ ---     \\
\bottomrule
\end{tabularx}
\end{table}

\begin{table}[H]
\caption{Multi-seed results on ETTm1 (mean $\pm$ std MSE). Three seeds: 2021, 42, 1234.\label{tab:multiseed_ettm1}}
\begin{tabularx}{\textwidth}{CCCCC}
\toprule
\textbf{Model} & \textbf{96} & \textbf{192} & \textbf{336} & \textbf{720} \\
\midrule
FTMamba  & 0.3441 $\pm$ 0.0022  & 0.3794 $\pm$ 0.0023  & 0.4068 $\pm$ ---     & \textit{pending}     \\
PatchTST & 0.3368 $\pm$ ---     & 0.3683 $\pm$ ---     & 0.4062 $\pm$ ---     & 0.4648 $\pm$ ---     \\
\bottomrule
\end{tabularx}
\end{table}

\begin{table}[H]
\caption{Multi-seed results on Weather (mean $\pm$ std MSE). Three seeds: 2021, 42, 1234.\label{tab:multiseed_weather}}
\begin{tabularx}{\textwidth}{CCCCC}
\toprule
\textbf{Model} & \textbf{96} & \textbf{192} & \textbf{336} & \textbf{720} \\
\midrule
FTMamba  & \textit{pending}     & \textit{pending}     & \textit{pending}     & \textit{pending}     \\
PatchTST & 0.1717 $\pm$ ---     & 0.2192 $\pm$ ---     & 0.2757 $\pm$ ---     & 0.3521 $\pm$ ---     \\
\bottomrule
\end{tabularx}
\end{table}

The multi-seed evaluation quantifies the variance introduced by random initialization and training stochasticity. On ETTh1, the standard deviation ranges from 0.0013 (T=192) to 0.0117 (T=720), with the largest relative variance at longer horizons. On ETTh2, the variance is similarly low (0.0019--0.0106). These low standard deviations relative to the inter-model gap confirm that FTMamba's gains are architectural rather than artifacts of a favorable seed. The results also resolve the discrepancy noted in Section~\ref{sec:discussion} between the main results (MSE 0.3776) and the ablation study (MSE 0.3826) on ETTh1/96, which we attribute to different random seeds across experimental runs. ETTm1 and Weather multi-seed results are partially available; complete results will be reported in the final version.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Discussion}\label{sec:discussion}

\subsection{Complementary Temporal and Frequency Information}

FTMamba's gains come from combining temporal and frequency streams rather than from either alone. Across four datasets and four horizons, the full model consistently beats single-branch variants (Table~\ref{tab:ablation}), supporting the hypothesis that the two domains carry complementary information. The temporal branch captures sequential dependencies and short-range dynamics, while the frequency branch exposes periodic structure that purely temporal models may miss.

\subsection{Gating Mechanism as the Key Component}

The gating mechanism matters more than the frequency branch itself. Removing the frequency branch costs only 0.9\% MSE, but replacing the gate with simple addition costs 4.0\% (Table~\ref{tab:ablation}). This gap has a practical interpretation: the frequency branch provides useful features, but only when the model can control how much to rely on them. Simple addition treats all inputs and time steps equally, which forces the model to compromise between temporal and frequency signals even when one is clearly more informative than the other.

The learned gate resolves this by adjusting the mixing ratio per input. For time series with strong periodicity (e.g., ETTh1 with daily and weekly cycles), the gate can upweight the frequency branch at longer horizons where periodic consistency matters. For series dominated by non-periodic fluctuations, it can suppress the frequency branch and rely on temporal modeling. This input-dependent adaptation explains why the gate contributes 4$\times$ more to accuracy than the frequency branch itself: the value of frequency features is contingent on context, and the gate provides that context~\cite{ref6,ref7}.

This finding parallels the role of gating in other architectures. In LSTMs, gates control information flow between cell states; in Mamba, the selective scan uses input-dependent parameters to control state updates. FTMamba's gate extends this principle across domains, controlling the balance between temporal and frequency representations rather than between time steps.

\subsection{Computational Efficiency}

Linear complexity gives FTMamba a practical edge over Transformer-based methods. PatchTST remains competitive on some horizons, but FTMamba's Mamba-based temporal branch processes sequences with $O(N \cdot D \cdot E)$ complexity, compared to the $O(N^2 \cdot D)$ cost of self-attention in PatchTST and iTransformer. This advantage grows with sequence length: for $N=13$ patches (our default configuration), the theoretical FLOP ratio is approximately 2.7:1 in favor of FTMamba, and the gap widens further for longer lookback windows.

\subsection{Dataset-Dependent Performance}

The model performs best on ETTh1 and ETTh2, where the temperature series contain clear periodic components (daily and weekly cycles). On ETTm1, which samples at 15-minute intervals and introduces finer-grained variation, PatchTST remains competitive at horizons 96--336. FTMamba leads only at T=720 on ETTm1. We attribute this to two factors: (1) the 4$\times$ higher sampling rate of ETTm1 produces more patches for the same lookback window, potentially diluting the frequency branch's ability to isolate meaningful periodic components; and (2) PatchTST's attention-based patches may better capture the high-frequency, non-periodic fluctuations that dominate short-horizon predictions on fine-grained data.

\subsection{Horizon-Dependent Gains}

The ablation results reveal that the temporal branch alone accounts for most of the forecasting accuracy. The frequency branch acts as a complement, not a replacement. Its value grows at longer horizons (336 and 720 steps), where maintaining periodic consistency over extended windows becomes harder for purely temporal models. At horizon 720 on ETTh1, FTMamba beats PatchTST by 4.3\% MSE--the largest margin in our experiments. On Weather, FTMamba achieves consistent gains at horizons 192--720, confirming that frequency-domain features help preserve periodic structure over long windows across different data domains.

\subsection{Training Variance}

A discrepancy exists between the main results and the ablation study: the full model reports MSE of 0.3776 on ETTh1/96 in Table~\ref{tab:etth1} but 0.3826 in Table~\ref{tab:ablation}. This 1.3\% gap stems from different random seeds used across experimental runs. The main results and ablation experiments were trained independently, and the seed-to-seed variance is comparable in magnitude to some of the gains between models (e.g., FTMamba vs.\ PatchTST at ETTh1/96 is also 1.3\%). Tables~\ref{tab:multiseed_etth1}--\ref{tab:multiseed_etth2} report multi-seed results to quantify this variance. On ETTh1, the standard deviation ranges from 0.0013 to 0.0117, which is well below the inter-model gap on most horizons. This confirms that FTMamba's improvements are architecturally grounded rather than seed-dependent. We report these results transparently to support reproducibility.

\subsection{Limitations}\label{sec:limitations}

The current frequency branch operates at a single FFT resolution determined by the patch count, which may not capture multi-scale periodic structure in high-frequency data like ETTm1. Wavelet-based or multi-resolution decomposition could address this. The learnable frequency filters and gate weights have not been analyzed for interpretability; understanding what frequency components the model emphasizes could provide domain insights. While we have added Weather as a fourth benchmark to demonstrate generalizability beyond the ETT family, evaluation on additional domains (traffic, electricity) would further confirm the dual-branch design's broad applicability. The lookback window is fixed at $L=96$ across all experiments; sensitivity to longer lookback windows (e.g., $L=336$) and to patch size $P$ and stride $S$ has not been explored. The model introduces additional parameters through the frequency branch and gating mechanism, though the overhead is modest compared to the temporal branch. The multi-seed reproducibility study (Tables~\ref{tab:multiseed_etth1}--\ref{tab:multiseed_weather}) covers ETTh1 and ETTh2 fully; ETTm1 and Weather results are partially available and will be completed in the final version.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Conclusions}

We presented FTMamba, a dual-branch architecture for long-term time series forecasting. A Mamba block handles temporal modeling at linear complexity; an FFT branch with learnable filters extracts periodic structure; a learned gate merges the two streams. On four benchmarks (ETTh1, ETTh2, ETTm1, Weather), FTMamba outperforms PatchTST, iTransformer, DLinear, TimesNet, and Transformer on 9 of 16 dataset--horizon pairs, with the largest margins at horizons 336 and 720.

The ablation study points to the gating mechanism as the more critical component: it accounts for a 4.0\% MSE drop when replaced by simple addition, while removing the entire frequency branch costs only 0.9\%. This suggests that how the model combines temporal and frequency information matters more than whether frequency features are present at all.

Future work includes extending the frequency branch to multi-resolution analysis (e.g., wavelet decomposition) for high-frequency data, interpreting the learned filter and gate weights for domain insights, and evaluating FTMamba on additional benchmarks from different domains (traffic, energy) to further confirm generalizability. Investigating sensitivity to lookback window length and patch configuration would further clarify the design space. The dual-branch design could also be adapted to other time series tasks such as imputation, anomaly detection, and classification.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\vspace{6pt}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\authorcontributions{Conceptualization, D.P. and H.Y.; methodology, H.Y.; software, H.Y.; validation, D.P., H.Y. and H.J.; formal analysis, H.Y.; investigation, H.Y.; resources, D.P.; data curation, H.Y.; writing---original draft preparation, H.Y.; writing---review and editing, D.P.; visualization, H.Y.; supervision, D.P.; project administration, D.P.; funding acquisition, D.P. All authors have read and agreed to the published version of the manuscript.}

\funding{This research was funded by the Guangdong Province Undergraduate Teaching Quality and Teaching Reform Project (grant no. Yue-Jiao-Gao-Han [2024] No. 30), the Guangdong Provincial Department of Education Provincial First-class Undergraduate Course (grant no. Yue-Jiao-Gao-Han [2023] No. 33), and the Shaoguan University Institutional Quality Engineering Project (grant no. Shao-Yuan-Jiao [2023] No. 30).}

\institutionalreview{Not applicable.}

\informedconsent{Not applicable.}

\dataavailability{The data used in this study are publicly available from the Time-Series-Library repository at https://github.com/thuml/Time-Series-Library.}

\acknowledgments{The authors thank the anonymous reviewers for their feedback.}

\conflictsofinterest{The authors declare no conflicts of interest.}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\abbreviations{Abbreviations}{
The following abbreviations are used in this manuscript:
\\

\noindent
\begin{tabular}{@{}ll}
LTSF & Long-Term Time Series Forecasting \\
SSM & State Space Model \\
FFT & Fast Fourier Transform \\
iFFT & Inverse Fast Fourier Transform \\
MSE & Mean Squared Error \\
MAE & Mean Absolute Error \\
ETT & Electricity Transformer Temperature \\
AMP & Automatic Mixed Precision
\end{tabular}
}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\begin{adjustwidth}{-\extralength}{0cm}

\reftitle{References}

\begin{thebibliography}{999}

\bibitem{ref1}
Rasp, S.; Thuerey, N. Data-driven medium-range weather prediction with a Resnet pretrained on climate simulations. \textit{J. Adv. Model. Earth Syst.} \textbf{2021}, \textit{13}, e2020MS002405. https://doi.org/10.1029/2020MS002405.

\bibitem{ref2}
Hong, T.; Pinson, P.; Fan, S.; et al. Probabilistic energy forecasting: Global Energy Forecasting Competition 2014 and beyond. \textit{Int. J. Forecast.} \textbf{2016}, \textit{32}, 896--913. https://doi.org/10.1016/j.ijforecast.2014.08.012.

\bibitem{ref3}
Li, Y.; Yu, R.; Shahabi, C.; Liu, Y. Diffusion convolutional recurrent neural network: Data-driven traffic forecasting. In Proceedings of the International Conference on Learning Representations (ICLR), Vancouver, BC, Canada, 30 April--3 May 2018. Available online: https://arxiv.org/abs/1707.01926 (accessed on 1 May 2026).

\bibitem{ref4}
Zhang, Z.; Zohren, S.; Roberts, S. DeepLOB: Deep convolutional neural networks for limit order books. \textit{IEEE Trans. Signal Process.} \textbf{2020}, \textit{68}, 3515--3530. https://doi.org/10.1109/TSP.2020.2989411.

\bibitem{ref5}
Vaswani, A.; Shazeer, N.; Parmar, N.; et al. Attention is all you need. In Proceedings of the Advances in Neural Information Processing Systems (NeurIPS), Long Beach, CA, USA, 4--9 December 2017; pp. 5998--6008. Available online: https://arxiv.org/abs/1706.03762 (accessed on 1 May 2026).

\bibitem{ref6}
Wu, H.; Xu, J.; Wang, J.; Long, M. Autoformer: Decomposition transformers with auto-correlation for long-term series forecasting. In Proceedings of the Advances in Neural Information Processing Systems (NeurIPS), Virtual, 6--14 December 2021; pp. 22419--22430. https://doi.org/10.48550/arXiv.2106.13008.

\bibitem{ref7}
Zhou, T.; Ma, Z.; Wen, Q.; et al. FEDformer: Frequency enhanced decomposed transformer for long-term series forecasting. In Proceedings of the International Conference on Machine Learning (ICML), Baltimore, MD, USA, 17--23 July 2022; pp. 27268--27286. https://doi.org/10.48550/arXiv.2201.12740.

\bibitem{ref8}
Nie, Y.; Nguyen, N.H.; Sinthong, P.; Kalagnanam, J. A time series is worth 64 words: Long-term forecasting with transformers. In Proceedings of the International Conference on Learning Representations (ICLR), Kigali, Rwanda, 1--5 May 2023. https://doi.org/10.48550/arXiv.2211.14730.

\bibitem{ref9}
Liu, Y.; Hu, T.; Zhang, H.; et al. iTransformer: Inverted transformers are effective for time series forecasting. In Proceedings of the International Conference on Learning Representations (ICLR), Vienna, Austria, 7--11 May 2024. https://doi.org/10.48550/arXiv.2310.06625.

\bibitem{ref10}
Gu, A.; Dao, T. Mamba: Linear-time sequence modeling with selective state spaces. \textit{arXiv} \textbf{2023}, arXiv:2312.00752. https://doi.org/10.48550/arXiv.2312.00752.

\bibitem{ref11}
Wang, S.; Zhang, H.; Li, L. S-Mamba: Mamba as a multivariate time series forecaster. \textit{arXiv} \textbf{2024}, arXiv:2403.11144. https://doi.org/10.48550/arXiv.2403.11144.

\bibitem{ref12}
Chen, Y.; Li, Z.; Wang, S. TimeMachine: A time series is worth 4 mamba blocks. \textit{arXiv} \textbf{2024}, arXiv:2403.03820. https://doi.org/10.48550/arXiv.2403.03820.

\bibitem{ref13}
Zeng, A.; Chen, M.; Zhang, L.; Xu, Q. Are transformers effective for time series forecasting? In Proceedings of the AAAI Conference on Artificial Intelligence, Washington, DC, USA, 7--14 February 2023; pp. 11121--11128. https://doi.org/10.1609/aaai.v37i9.26317.

\bibitem{ref14}
Wu, H.; Hu, T.; Liu, Y.; et al. TimesNet: Temporal 2D-variation modeling for general time series analysis. In Proceedings of the International Conference on Learning Representations (ICLR), Kigali, Rwanda, 1--5 May 2023. https://doi.org/10.48550/arXiv.2210.02186.

\bibitem{ref15}
Gu, A.; Goel, K.; Ré, C. Efficiently modeling long sequences with structured state spaces. In Proceedings of the International Conference on Learning Representations (ICLR), Virtual, 25--29 April 2022. https://doi.org/10.48550/arXiv.2111.00396.

\bibitem{ref16}
Yi, K.; Zhang, Q.; Fan, W.; et al. Frequency-domain MLPs are more effective learners in time series forecasting. In Proceedings of the Advances in Neural Information Processing Systems (NeurIPS), New Orleans, LA, USA, 10--16 December 2024. https://doi.org/10.48550/arXiv.2311.06967.

\bibitem{ref17}
Zhou, H.; Zhang, S.; Peng, J.; et al. Informer: Beyond efficient transformer for long sequence time-series forecasting. In Proceedings of the AAAI Conference on Artificial Intelligence, Virtual, 2--9 February 2021; pp. 11106--11115. https://doi.org/10.1609/aaai.v35i12.17325.

\bibitem{ref18}
Wu, H.; Hu, T.; Liu, Y.; et al. Time-Series-Library (v1.0.0). Zenodo \textbf{2023}. https://doi.org/10.5281/zenodo.8264358.

\bibitem{ref19}
Kingma, D.P.; Ba, J. Adam: A method for stochastic optimization. In Proceedings of the International Conference on Learning Representations (ICLR), San Diego, CA, USA, 7--9 May 2015. https://doi.org/10.48550/arXiv.1412.6980.

\end{thebibliography}

\PublishersNote{}
\end{adjustwidth}

\end{document}
