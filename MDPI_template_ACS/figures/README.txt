Architecture Figure for FTMamba Paper

Please create a PDF figure named "architecture.pdf" with the following content:

1. Input Time Series at the top
2. Patch Embedding layer
3. Stacked FTMamba Layers (show 2-3 layers)
   Each FTMamba Layer should show:
   - Temporal Branch (Mamba Block)
   - Frequency Branch (FFT + Learnable Filter + iFFT)
   - Gated Fusion Mechanism
4. Prediction Head at the bottom
5. Output Forecast

The figure should clearly show:
- The dual-branch architecture
- How temporal and frequency features are combined
- The flow of data through the model
- Residual connections between layers

Recommended tools: draw.io, PowerPoint, or TikZ (LaTeX)

The figure should be in PDF format and sized to fit in a single column (approximately 8cm wide).
